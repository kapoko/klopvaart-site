import path from "node:path";
import defaultConfig from "@wordpress/scripts/config/webpack.config";

const config = {
  ...defaultConfig,
  entry: {
    variation: path.resolve(__dirname, "src/variation/index.ts"),
  },
};

export default config;
